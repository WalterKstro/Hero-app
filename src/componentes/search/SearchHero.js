import SearchForm from "./SearchForm"

const SearchHero = ({history}) => {
    return (
        <>
            <SearchForm 
                history={history}
            />
        </>
    )
}

export default SearchHero
